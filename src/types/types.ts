export type Country = {
  albumId: number;
  id: number;
  title: string;
  url: string;
  thumbnailUrl: string;
};

export type Countrys = {
  albumId: number;
  id: number;
  title: string;
  url: string;
  thumbnailUrl: string;
};
